{
  "name": "my-v0-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "html-minifier-terser index.html --collapse-whitespace --remove-comments --minify-css true --minify-js true --remove-optional-tags --remove-attribute-quotes -o dist/index.html"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
